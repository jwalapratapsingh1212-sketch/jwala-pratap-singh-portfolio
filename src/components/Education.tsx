import { usePortfolio } from '../hooks'
export function Education(){const {education}=usePortfolio();return <section id="education"><div className="container"><div className="section-kicker">05 / Education</div><h2 className="section-title">Learning with intent.</h2>{education.map((e:any)=><div className="education-card" key={e.degree}><div className="eyebrow">{e.period}</div><h3>{e.degree}</h3><p className="muted">{e.institution}<br/>{e.detail}</p></div>)}<div style={{marginTop:45}}><div className="section-kicker">CURRENTLY EXPLORING</div><p className="muted" style={{maxWidth:760,fontSize:18}}>Machine Learning, Deep Learning, NLP, Generative AI, LLMs and AI Agent Development through hands-on learning, hackathons and research-oriented work.</p></div></div></section>}
